name: land_photo
description: LandPhoto Flutter client
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.2.0
  http: ^1.2.2
  connectivity_plus: ^6.0.3
  cached_network_image: ^3.4.1
  transparent_image: ^2.0.1
  emoji_picker_flutter: ^3.1.0
  image_picker: ^1.1.2
  path: ^1.9.0
  path_provider: ^2.1.3
  lottie: ^2.7.0

  # ????
  supabase_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/status.json