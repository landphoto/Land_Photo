name: land_photo
description: Land Photo with online update prompts and maintenance mode
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/
